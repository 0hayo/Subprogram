<view class="tabs">
  <view class="tabs_title">
    <view class="tabs_item {{item.isActive ? 'isActive' : ''}}" wx:for="{{tabs}}" bindtap="handleTab" data-index="{{index}}">
      {{item.name}}
    </view>
  </view>
  <view class="tabs_content">
    <slot></slot>
  </view>
</view>